streamlit>=1.20
google-genai>=1.0.0   # official modern GenAI SDK (recommended)
pydub
python-dotenv
python-docx
fpdf
tqdm
# For Streamlit Cloud and other PaaS where system ffmpeg isn't present,
# include imageio-ffmpeg which provides a bundled ffmpeg binary.
imageio-ffmpeg


